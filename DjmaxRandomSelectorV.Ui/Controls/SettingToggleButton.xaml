<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:DjmaxRandomSelectorV.Ui.Controls">

    <Style TargetType="{x:Type controls:SettingToggleButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:SettingToggleButton}">
                    <ItemsControl Style="{DynamicResource StyleSettingBar}">
                        <ItemsControl.Resources>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </ItemsControl.Resources>
                        <Border Style="{DynamicResource StyleClearBox}"
                                Padding="{DynamicResource PaddingSettingBarLabelArea}">
                            <TextBlock Text="{Binding Label, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Border>
                        <Grid Grid.Column="1">
                            <TextBlock x:Name="ToggleButtonText"
                                       Text="{Binding TextWhenNotChecked, RelativeSource={RelativeSource TemplatedParent}}"
                                       Foreground="{DynamicResource ColorUnselected}"
                                       HorizontalAlignment="Center"/>
                            <ToggleButton IsChecked="{Binding IsChecked, RelativeSource={RelativeSource TemplatedParent}}"
                                          Style="{DynamicResource StyleTransparentToggleButton}"/>
                        </Grid>
                    </ItemsControl>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Text"
                                    TargetName="ToggleButtonText"
                                    Value="{Binding TextWhenChecked, RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Setter Property="Foreground"
                                    TargetName="ToggleButtonText"
                                    Value="{DynamicResource ColorSelected}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>