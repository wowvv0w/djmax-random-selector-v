<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:DjmaxRandomSelectorV.Ui.Controls">
    <Style TargetType="{x:Type controls:SettingSpinBox}">
        <Setter Property="ValueText" Value="{Binding Value, RelativeSource={RelativeSource Self}}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:SettingSpinBox}">
                    <ItemsControl Style="{DynamicResource StyleSettingBar}">
                        <ItemsControl.Resources>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </ItemsControl.Resources>
                        <Border Style="{DynamicResource StyleClearBox}"
                                Padding="{DynamicResource PaddingSettingBarLabelArea}">
                            <TextBlock Text="{Binding Label, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Border>
                        <Grid Grid.Column="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="5*"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <RepeatButton x:Name="PrevValueButton"
                                          Grid.Column="0"
                                          Delay="{Binding Delay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Interval="{Binding Interval, RelativeSource={RelativeSource TemplatedParent}}"
                                          Style="{DynamicResource StyleSettingSpinBoxLeftRepeatButton}"/>
                            <TextBlock Grid.Column="1"
                                       Text="{TemplateBinding ValueText}"
                                       Foreground="{DynamicResource ColorSelected}"
                                       HorizontalAlignment="Center"/>
                            <RepeatButton x:Name="NextValueButton"
                                          Grid.Column="2"
                                          Delay="{Binding Delay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Interval="{Binding Interval, RelativeSource={RelativeSource TemplatedParent}}"
                                          Style="{DynamicResource StyleSettingSpinBoxRightRepeatButton}"/>
                        </Grid>
                    </ItemsControl>
                    
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>