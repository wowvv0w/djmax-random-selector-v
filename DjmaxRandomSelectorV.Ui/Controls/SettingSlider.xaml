<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:DjmaxRandomSelectorV.Ui.Controls">
    <Style TargetType="{x:Type controls:SettingSlider}">
        <Setter Property="ValueText" Value="{Binding Value, RelativeSource={RelativeSource Self}}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:SettingSlider}">
                    <ItemsControl Style="{DynamicResource StyleSettingBar}">
                        <ItemsControl.Resources>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </ItemsControl.Resources>
                        <Border Style="{DynamicResource StyleClearBox}"
                                Padding="{DynamicResource PaddingSettingBarLabelArea}">
                            <Grid>
                                <TextBlock Text="{Binding Label, RelativeSource={RelativeSource TemplatedParent}}"/>
                                <TextBlock Text="{TemplateBinding ValueText}"
                                           HorizontalAlignment="Right"/>
                            </Grid>
                        </Border>
                        <Grid Grid.Column="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="10*"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Slider Grid.Column="1"
                                    Value="{Binding Value, RelativeSource={RelativeSource TemplatedParent}}"
                                    Minimum="{Binding Minimum, RelativeSource={RelativeSource TemplatedParent}}"
                                    Maximum="{Binding Maximum, RelativeSource={RelativeSource TemplatedParent}}"
                                    TickFrequency="{Binding TickFrequency, RelativeSource={RelativeSource TemplatedParent}}"
                                    IsSnapToTickEnabled="{Binding IsSnapToTickEnabled, RelativeSource={RelativeSource TemplatedParent}}"
                                    Style="{DynamicResource StyleSettingSlider}"/>
                        </Grid>
                    </ItemsControl>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>