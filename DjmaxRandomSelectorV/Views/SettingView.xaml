<Window x:Class="DjmaxRandomSelectorV.Views.SettingView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DjmaxRandomSelectorV.Views"
        xmlns:controls="clr-namespace:DjmaxRandomSelectorV.Views.Controls"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Width="660" Height="440"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Border Background="#EF000000"
            BorderBrush="Gray"
            BorderThickness="2">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="10"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="20"/>
                <RowDefinition/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="10"/>
            </Grid.RowDefinitions>

            <!--#region Body-->
            <ScrollViewer VerticalScrollBarVisibility="Visible"
                          Grid.Row="1" Grid.Column="1"
                          Style="{StaticResource PlaylistScrollViewer}"
                          Margin="40 0 27 0"
                          Padding="0 0 5 0">
                <ScrollViewer.Resources>
                    <sys:Double x:Key="{x:Static SystemParameters.VerticalScrollBarWidthKey}">8</sys:Double>
                </ScrollViewer.Resources>
                <StackPanel Orientation="Vertical">

                    <!--Selector Setting-->
                    <TextBlock Text="SELECTOR SETTING"
                               Style="{StaticResource SettingCategoryText}"/>
                    <controls:SettingToggleButton DataContext="{Binding IsPlaylistUpdater}"/>
                    <controls:SettingSlider DataContext="{Binding InputDelayUpdater}"/>
                    <controls:SettingToggleButton DataContext="{Binding SavesRecentsUpdater}"/>

                    <!--Track List Setting-->
                    <Grid Style="{x:Null}" Margin="0 10 0 0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.Resources>
                            <Style TargetType="Button" BasedOn="{StaticResource SettingWindowButton}">
                                <Setter Property="BorderThickness" Value="2"/>
                                <Setter Property="Border.CornerRadius" Value="4"/>
                                <Setter Property="FontSize" Value="10"/>
                                <Setter Property="Width" Value="50"/>
                                <Setter Property="Height" Value="20"/>
                            </Style>
                        </Grid.Resources>
                        <TextBlock Text="TRACK LIST SETTING"
                                   Style="{StaticResource SettingCategoryText}"/>
                        <Button x:Name="DetectDlcs"
                                Content="DETECT"
                                Grid.Column="2"/>
                    </Grid>
                    <ItemsControl x:Name="CategoryUpdaters">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <controls:SettingToggleButton/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
            <!--#endregion-->

            <!--#region Footer-->
            <StackPanel Orientation="Horizontal"
                        Grid.Row="3" Grid.Column="1"
                        HorizontalAlignment="Right"
                        Margin="40 0">
                <StackPanel.Resources>
                    <Style TargetType="{x:Type Button}" BasedOn="{StaticResource SettingWindowButton}">
                        <Setter Property="Width" Value="80"/>
                    </Style>
                </StackPanel.Resources>
                <Button x:Name="Apply"
                        Content="APPLY"/>
                <Button x:Name="Cancel"
                        Content="CANCEL"/>
            </StackPanel>
            <!--#endregion-->

        </Grid>
        
    </Border>

</Window>
